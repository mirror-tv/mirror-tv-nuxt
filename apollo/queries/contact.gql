query fetchContactBySlug($slug: String!) {
  allContacts(
    where: {
      slug: $slug
    }
  ) {
    name
    image {
      urlMobileSized
    }
    facebook
    instatgram
    bio
  }
}

query fetchContactByAnchorPerson {
  allContacts(
    where: {
      anchorperson: true
    }
  ) {
    name
    image {
      urlMobileSized
    }
    slug
  }
}
