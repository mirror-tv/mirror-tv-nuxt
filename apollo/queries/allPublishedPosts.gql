query allPublishedPosts($first: Int = 12, $skip: Int = 0) {
  allPublishedPosts: allPosts(
    sortBy: publishTime_DESC,
    where: {
      state: published
    },
    first: $first,
    skip: $skip
  ) {
    id
    title
    slug
    publishTime
    categories {
      title
    }
    heroImage {
      urlMobileSized
    }
  }

  meta: _allPostsMeta(
    sortBy: publishTime_DESC,
    where: {
      state: published
    }
  ) {
    count
  }
}
